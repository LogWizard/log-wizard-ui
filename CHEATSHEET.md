# 🧙‍♂️ Log-Wizard UI - Cheatsheet
> Адмін-панель для твоїх ботів. Бач все, керуй всім. 🌿🚬

## 🚬 Суть
Візуалізація логів Telegram-ботів та можливість ручного втручання.
- **Бекенд:** Node.js + Express
- **Фронтенд:** Modern Web App (`frontend/` folder)
- **БД:** MariaDB (читає таблиці, які наповнюють боти)
- **Manual Mode:** Глобальний перемикач AI/Manual для конкретних чатів.

## 🛠 Архітектура
1. `src/server.js` — API сервер.
2. `frontend/` — Клієнтська частина.
3. `config/manual_mode.json` — Файл-конфіг для перемикання режимів.
4. `src/routes/` — Ендпоінти для отримання повідомлень та керування.

## 🚀 КОМАНДИ
- `npm run dev` — Запуск тільки бекенда.
- `npm run dev:all` — Запуск бекенда + фронтенда (HMR).
- `npm run build` — Збірка фронтенда.

## 🚨 ОСОБЛИВОСТІ
1. **Manual Mode:** Коли ти вмикаєш "Manual" в UI, бот (напр. GYS) бачить це в `config/manual_mode.json` і мовчить, поки ти не відповіси через UI або сам в TG.
2. **Media:** Підтримка відображення фото/стікерів/голосух. Юзає `ffmpeg` для обробки. (Залежності: `@ffmpeg-installer/ffmpeg`).

## 🗂 Структура
```
log-wizard-ui/
  frontend/         — React/Vue додаток
  src/
    server.js       — Експрес сервер
    routes/         — API логіка
  config/
    manual_mode.json — статус ручного керування
  public/           — статичні файли
```

## 🌐 Порти
- Бекенд API: зазвичай `:2222` (перевір `.env`)
- Фронтенд Dev: `:5173` (Vite)

Будь чарівником своїх логів! 🧙‍♂️🌿🚬
